rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /allotments/{doc} {
      allow read, write: if request.auth != null;
    }
    match /expenses/{doc} {
      allow read, write: if request.auth != null;
    }
  }
}
